export enum SessionTypes {
    PRIVATE = 'private',
    COMMUNITY = 'community'
}

export enum SessionStatus {
    PENDING = 'pending',
    CONFIRMED = 'confirmed',
    CANCELED = 'canceled',
    COMPLETED = 'completed'
}